server:
  port: 8084

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8061/eureka/

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"

springdoc:
  packagesToScan: com.comit.services.employee

spring:
  datasource:
    username: tung
    url: jdbc:mysql://localhost:3306/employee_db?autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update

core:
  api:
    add-employee-image: http://10.10.2.112:5678/embedding/
    update-employee-image: http://10.10.2.112:5678/embedding
    delete-employee-image: http://10.10.2.112:5678/embedding